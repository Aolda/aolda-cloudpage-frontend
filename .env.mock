# 백엔드 없이 프론트 mock API로 테스트
# 사용법: 이 파일을 .env.local 로 복사하거나 `yarn dev:mock` 실행
USE_MOCK_API=true
NEXT_PUBLIC_USE_MOCK_API=true

# mock 모드에서는 실제 호출하지 않지만, 실 API 전환 시를 위해 유지
NEXT_PUBLIC_API_BASE_URL=http://localhost:8001
